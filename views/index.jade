extends layout

block content
  h1 Train Schedule
  p Select your train below to see its current schedule_changed to test git commands.
  #wrapper
    #trainList
      h2 Trains
      #trains.d-flex.flex-row
    #trainInfo
      strong  <span id='trainName'></span>
      #trainSchedule
        strong Select a train to view its current schedule.
